# Security policy

Security fixes are applied to the latest release.

Use GitHub private vulnerability reporting for security issues. Never include real conversation content, credentials, usernames, or personal paths in a public issue. A minimal fictional fixture is preferred.

Safety controls include UUID validation, loopback-only services, blocked external WebKit navigation, argument-array subprocess calls, non-overwriting archive behavior, local delete backups, and read-only refresh tests.
