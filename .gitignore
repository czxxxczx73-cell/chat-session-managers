# 构建时在本机生成的隔离环境（含编译后的依赖，不能跨机器复用，也不该进 git）
.build/
dist/
.release-stage/
venv/
.venv/

# Python 缓存
__pycache__/
*.pyc

# 生成的 App 本体——由 scripts/build-native.sh 现场生成，不提交打包好的产物
*.app/

# macOS / 编辑器杂项
.DS_Store
*.log
