name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate-release:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Audit and test read-only refresh
        run: make check test
      - name: Build the release package
        run: make package
      - name: Verify artifacts
        run: |
          file dist/native/*.app/Contents/MacOS/SessionManagerHost
          ls -lh dist/*.zip
